replicaCount: 3

image:
  repository: nginx
  tag: "1.25-alpine"
  pullpolicy: IfNotPresent

service:
  type: LoadBalancer
  port: 80

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 250m

autoscaling:
  enabled: true
  minReplicas: 2
  maxReplicas: 10
